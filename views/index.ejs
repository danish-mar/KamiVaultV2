<style>
    @keyframes fade-up {
        from { opacity: 0; transform: translateY(24px); }
        to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes subtle-glow {
        0%, 100% { opacity: 0.4; transform: scale(1); }
        50%       { opacity: 0.7; transform: scale(1.05); }
    }
    .fade-up-1 { animation: fade-up .8s cubic-bezier(.16,1,.3,1) .1s both; }
    .fade-up-2 { animation: fade-up .8s cubic-bezier(.16,1,.3,1) .25s both; }
    .fade-up-3 { animation: fade-up .8s cubic-bezier(.16,1,.3,1) .4s both; }
    .fade-up-4 { animation: fade-up .8s cubic-bezier(.16,1,.3,1) .55s both; }
    .glow-bg   { animation: subtle-glow 8s ease-in-out infinite; }
</style>

<!-- ── Ambient glow ── -->
<div class="pointer-events-none fixed inset-0 overflow-hidden" aria-hidden="true">
    <div class="glow-bg absolute top-[-20%] left-1/2 -translate-x-1/2 w-[800px] h-[600px] rounded-full bg-purple-100/50 blur-[120px]"></div>
</div>

<!-- ── Hero ── -->
<section class="relative flex flex-col items-center text-center px-6 pt-32 pb-24 min-h-[90vh] justify-center">

    <!-- eyebrow -->
    <div class="fade-up-1 inline-flex items-center space-x-2 mb-8 px-4 py-1.5 rounded-full bg-white border border-gray-100 shadow-sm">
        <span class="h-1.5 w-1.5 rounded-full bg-emerald-500 shadow-[0_0_6px_rgba(34,197,94,0.6)]"></span>
        <span class="text-[11px] font-semibold text-gray-500 tracking-wide">Document intelligence, ready now</span>
    </div>

    <!-- headline -->
    <h1 class="fade-up-2 text-[clamp(2.4rem,6vw,4.5rem)] font-bold tracking-tight text-gray-900 leading-[1.08] max-w-3xl">
        Your paper records,<br/>
        <span class="text-purple-600">beautifully digital.</span>
    </h1>

    <!-- sub -->
    <p class="fade-up-3 mt-6 text-[clamp(14px,2vw,17px)] text-gray-500 font-light leading-relaxed max-w-xl">
        KamiVault scans, extracts, and organises every field from your physical documents — instantly searchable, securely stored, shareable.
    </p>

    <!-- CTA row -->
    <div class="fade-up-4 mt-10 flex flex-col sm:flex-row items-center gap-3">
        <a href="/auth/register"
           class="px-8 py-3 bg-gray-900 text-white text-[14px] font-semibold rounded-2xl hover:bg-black transition-all shadow-lg shadow-gray-900/10 flex items-center gap-2 group">
            Get Started — it's free
            <i class="fas fa-arrow-right text-[11px] group-hover:translate-x-0.5 transition-transform"></i>
        </a>
        <a href="/auth/login"
           class="px-8 py-3 bg-white text-gray-600 text-[14px] font-medium rounded-2xl border border-gray-200 hover:border-gray-300 hover:bg-gray-50 transition-all">
            Sign In
        </a>
    </div>

    <!-- scroll hint -->
    <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center space-y-1 opacity-30">
        <span class="text-[10px] font-medium text-gray-400 uppercase tracking-widest">Scroll</span>
        <i class="fas fa-chevron-down text-gray-400 text-[10px]"></i>
    </div>
</section>

<!-- ── Divider ── -->
<div class="w-full h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent"></div>

<!-- ── Three Pillars ── -->
<section class="px-6 md:px-16 py-24 max-w-5xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">

        <div class="group space-y-5">
            <div class="h-11 w-11 bg-purple-50 text-purple-600 rounded-2xl flex items-center justify-center text-lg group-hover:bg-purple-600 group-hover:text-white transition-all duration-500 shadow-sm">
                <i class="fas fa-bolt"></i>
            </div>
            <div class="space-y-2">
                <h3 class="text-[15px] font-semibold text-gray-900">Instant Extraction</h3>
                <p class="text-[13px] text-gray-400 leading-relaxed font-light">Upload any document. AI reads every field — names, dates, IDs — in seconds.</p>
            </div>
        </div>

        <div class="group space-y-5">
            <div class="h-11 w-11 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center text-lg group-hover:bg-blue-600 group-hover:text-white transition-all duration-500 shadow-sm">
                <i class="fas fa-shield-halved"></i>
            </div>
            <div class="space-y-2">
                <h3 class="text-[15px] font-semibold text-gray-900">Private by Default</h3>
                <p class="text-[13px] text-gray-400 leading-relaxed font-light">Your originals stay in your own secure cloud vault. No third-party access.</p>
            </div>
        </div>

        <div class="group space-y-5">
            <div class="h-11 w-11 bg-emerald-50 text-emerald-600 rounded-2xl flex items-center justify-center text-lg group-hover:bg-emerald-600 group-hover:text-white transition-all duration-500 shadow-sm">
                <i class="fas fa-user-group"></i>
            </div>
            <div class="space-y-2">
                <h3 class="text-[15px] font-semibold text-gray-900">Built for Teams</h3>
                <p class="text-[13px] text-gray-400 leading-relaxed font-light">Share collections with colleagues. Set precise access roles — viewer, editor, or admin.</p>
            </div>
        </div>

    </div>
</section>

<!-- ── Divider ── -->
<div class="w-full h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent"></div>

<!-- ── Stats strip ── -->
<section class="px-6 md:px-16 py-16 max-w-5xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 text-center md:text-left">
        <div class="space-y-1">
            <p class="text-[36px] font-bold text-gray-900 tracking-tight">50<span class="text-purple-500">×</span></p>
            <p class="text-[13px] text-gray-400 font-light">Documents processed per batch</p>
        </div>
        <div class="space-y-1">
            <p class="text-[36px] font-bold text-gray-900 tracking-tight">99.9<span class="text-emerald-500">%</span></p>
            <p class="text-[13px] text-gray-400 font-light">AI accuracy on structured records</p>
        </div>
        <div class="space-y-1">
            <p class="text-[36px] font-bold text-gray-900 tracking-tight"><span class="text-blue-500">∞</span></p>
            <p class="text-[13px] text-gray-400 font-light">Storage, secured in your private cloud</p>
        </div>
    </div>
</section>

<!-- ── Divider ── -->
<div class="w-full h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent"></div>

<!-- ── Final CTA ── -->
<section class="px-6 py-28 text-center space-y-6">
    <h2 class="text-[clamp(1.5rem,4vw,2.5rem)] font-bold text-gray-900 tracking-tight">
        Start in under a minute.
    </h2>
    <p class="text-[14px] text-gray-400 font-light max-w-sm mx-auto">No setup. No configuration. Just upload and let the AI do the rest.</p>
    <a href="/auth/register"
       class="inline-flex items-center gap-2 px-8 py-3 bg-purple-600 text-white text-[14px] font-semibold rounded-2xl hover:bg-purple-700 transition-all shadow-lg shadow-purple-200 group mt-2">
        Create your Vault
        <i class="fas fa-arrow-right text-[11px] group-hover:translate-x-0.5 transition-transform"></i>
    </a>
</section>
